package sda2

// Daemon is the main structure that is to be created to run a SDA daemon.
type Daemon struct {
	Router
}

// Client is the main structure that is to be created to run a SDA client.
type Client struct {
}
